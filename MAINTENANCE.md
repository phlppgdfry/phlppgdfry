# Profile maintenance

[← Profile](README.md)

## Content map

- `README.md`: curated front page, six featured projects and generated public activity.
- `INDIE_DEV.md`, `BA_PROFILE.md`, `PROFESSIONAL.md`, `PERSONAL.md`: four reading routes.
- `PROJECTS.md`: full catalog, reviewed on 2026-09-09; update classifications when project scope changes.
- `TOOLBOX.md`: extended technology list retained from the earlier profile.
- `assets/`: repository-owned banners, original GIFs, activity card and project evidence.

## Update activity

The daily GitHub Actions workflow runs at approximately 06:23 UTC. It can also be started with **Actions → Refresh profile activity → Run workflow**.

Locally, run `python3 scripts/update_activity.py` with an authenticated GitHub CLI, or provide `GH_TOKEN`. The script uses only public repositories, excludes forks and this profile, and retains the previous snapshot if collection fails. It updates the marked activity section and `assets/activity.svg`.

The six-repository release watchlist is explicit in the script and README. The project catalog, featured selection, maturity labels and workbench are editorial; they are not silently inferred from commits.

## Rebuild artwork

Install Pillow in a virtual environment, then run `python3 scripts/build_visuals.py`. The generator uses Menlo on macOS or DejaVu Sans Mono on Linux. Banners are SVG; animations are small looping GIFs. All essential content also appears as text.

## Asset provenance

- `banner.svg`, the four page banners, `harbor.gif`, `terminal.gif`, `pipeline.gif`, `radar.gif` and `coast.gif`: original geometric artwork generated by `scripts/build_visuals.py` for this profile.
- `activity.svg`: generated from public GitHub metadata.
- `clicktrack.png`: copied from `phlppgdfry/ClickTrack/assets/screenshots/clicktrack-live-today.png`.
- `mirrormate.png`: copied from `phlppgdfry/MirrorMate-App/assets/screenshot-appearance.png`.
- `docurelay-demo.gif`: copied from `phlppgdfry/docurelay-field/docs/assets/android-runtime-postfix-demo.gif`, a recorded Android emulator workflow.
- Two GIPHY embeds are retained from the previous profile. They are externally hosted entertainment, not project demonstrations or original artwork.

## Suggested pinned repositories

Recommended order: **Logistics-Master**, **portops-ai**, **ClickTrack**, **MirrorMate-App**, **docurelay-field**, **fabric-lottery-data-platform**. The README already presents this selection. GitHub's profile pin selection is separate from this repository.

## Review before publishing

Check local links, Markdown tables, image alt text, GIF frame counts, SVG validity and the rendered page in light/dark themes and at narrow widths. Keep factual claims aligned with source project READMEs, especially release status and portfolio/scaffold distinctions.
